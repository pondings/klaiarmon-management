import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.klaiarmon.management',
  appName: 'KM',
  webDir: 'dist/klaiarmon-management',
  bundledWebRuntime: false
};

export default config;
