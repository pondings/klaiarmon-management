import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.klaiarmon.management',
  appName: 'KM',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
